package com.sithum.safevoice.enums;

/** Reason selected by the reporter, stored on {@code reports.reason}. */
public enum ReportReason {
    SPAM,
    HARASSMENT,
    MISINFORMATION,
    HATE_SPEECH,
    OTHER
}
